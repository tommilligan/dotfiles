- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.gitconfig:
    ~/.gitconfig-user:
    ~/.tmux.conf:
    ~/.ssh/config: ssh-config
    ~/.xinitrc:
    ~/.vimrc:

- shell:
  - [git submodule update --init --recursive, Installing submodules]
